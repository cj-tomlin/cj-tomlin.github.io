---
import { Button } from "@/components/starwind/button";
const { platform, username } = Astro.props;
---

<Button variant="primary" href={`https://www.${platform}.com/${username}`}>
  {platform}
</Button>
